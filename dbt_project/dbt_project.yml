name: 'australian_companies'
version: '1.0.0'
config-version: 2

profile: 'australian_companies'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  australian_companies:
    staging:
      materialized: view
    marts:
      materialized: table